import React from "react";
import EventListContentContainer from "./EventListContent.container";

const EventListContent = (props) => {
  return <EventListContentContainer {...props} />;
};

export default EventListContent;